{
  ".worktrees/context-decimal/packages/plugin/src/package.ts::PluginPackage": {
    "specs": [
      {
        "cases": [],
        "spec": "A Plugin Package does not declare scope or namespace; one enabled PluginInstance activates one Worker, while each Resource carries its own namespace."
      }
    ]
  },
  ".worktrees/context-decimal/packages/plugin/src/reconcile-context.ts::AskInput": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep allowOther as the type discriminant; allowOther:true must never expose a choice-only result type."
        ],
        "spec": "Closed-choice asks preserve their choice value union, while any ask that permits free text returns string because the answer may be outside those choices."
      }
    ]
  },
  ".worktrees/context-decimal/packages/plugin/src/reconcile-context.ts::PluginVerbs": {
    "specs": [
      {
        "cases": [],
        "links": [
          "docs/plugin.md"
        ],
        "rules": [
          "Correlate verb continuation with the Core-issued execution identity, never with Resource identity or a caller-provided key; a crashed execution fails instead of replaying its call stack.",
          "Require one total timeout for every verb; draft and harness deadlines cover their Interaction gate and the resulting HarnessInvocation through terminal Turn."
        ],
        "spec": "Every Plugin verb first persists a Core-owned Interaction and suspends the same live Plugin execution until a terminal result; Baton never re-enqueues a Resource merely to deliver that result."
      }
    ]
  },
  ".worktrees/context-decimal/packages/plugin/src/reconcile-context.ts::VerbResult": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Treat deliberate negative answers as successful business values; reserve dismissed for Esc/close and failure for infrastructure or execution errors."
        ],
        "spec": "Every Plugin verb has exactly four terminal outcomes: success with a business value, explicit user dismissal, deadline timeout, or execution failure."
      }
    ]
  },
  ".worktrees/context-decimal/packages/plugin/src/resource.ts::ResourceClient": {
    "specs": [
      {
        "cases": [],
        "spec": "One PluginInstance may own Resources in global, Project, and Session namespaces simultaneously; omitted mutation namespace defaults to `v1`."
      }
    ]
  },
  ".worktrees/context-decimal/packages/plugin/src/resource.ts::ResourceClient.get": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "After awaiting a Core verb, use a uid-pinned ref before applying the result so a stale continuation cannot write to a replacement Resource."
        ],
        "spec": "A ResourceRef lookup returns the latest Resource only within the referenced namespace and, when uid is present, only for that exact incarnation; deletion or replacement returns undefined."
      }
    ]
  },
  ".worktrees/context-decimal/src/commands/registry.ts::CommandRegistry.resolve": {
    "specs": [
      {
        "cases": [],
        "spec": "Alias 只改变 slash 调用形式：它先绑定 canonical Command 参数；无剩余命令参数且显式允许 trailing text 的 Commandable，才把后缀作为独立 Prompt 返回。"
      }
    ]
  },
  ".worktrees/context-decimal/src/controller/index.ts::Controller.sendTurn": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../../docs/workflow.md"
        ],
        "spec": "每次用户提交只创建一个 Input/messageId；steer rejection 降级 follow-up 时保持身份不变。"
      }
    ]
  },
  ".worktrees/context-decimal/src/interaction/reconcile.ts::ReconcileInteractionStore": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep the Event Ledger as the durable fact store, not a continuation store: recovery must fail orphaned Plugin Interactions instead of reviving an old call stack."
        ],
        "spec": "A Plugin Interaction appends its first terminal fact to the Event Ledger before resolving any suspended continuation; later terminal attempts cannot replace it."
      }
    ]
  },
  ".worktrees/context-decimal/src/plugin/queue.ts::ReconcileCapacityLease": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "A Plugin verb wait must suspend both the Controller-local and Manager-global leases so one human wait cannot block unrelated Resources at either limit."
        ],
        "spec": "Suspending a reconcile releases its capacity slot for the full external wait and reacquires a slot before the original continuation resumes."
      }
    ]
  },
  ".worktrees/context-decimal/src/plugin/runner/client.ts::PluginRunnerClient": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Exclude a pending host-side Plugin verb from the generic Runner watchdog because the verb owns that wait's timeout; re-arm the watchdog after the host reply.",
          "Restore the host async causal scope by executionId before handling a Runner verb callback."
        ]
      }
    ]
  },
  ".worktrees/context-decimal/src/queue.ts::Queue.claimFirstForSteer": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../docs/workflow.md"
        ],
        "spec": "只有仍位于队头的 HarnessInput 才能被 same-turn dispatch claim；claim 后不可被 composer recall。"
      }
    ]
  },
  ".worktrees/context-decimal/src/view/chat-tui/protocol/index.ts::BatonChatProtocol.command": {
    "specs": [
      {
        "cases": [],
        "spec": "一次 Commandable 提交先完整执行并结算 canonical Command；只有成功后，允许的 trailing text 才作为下一条普通 Prompt 提交。"
      }
    ]
  },
  ".worktrees/context-decimal/src/view/chat-tui/protocol/state.ts::projectChatState": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Order status segments by volatility: stable model and configuration precede active phase or tool activity, while changing observations such as context usage stay at the end so frequent updates disturb only the right side."
        ]
      }
    ]
  },
  ".worktrees/control-theory-docs/packages/plugin/src/reconcile-context.ts::AskInput": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep allowOther as the type discriminant; allowOther:true must never expose a choice-only result type."
        ],
        "spec": "Closed-choice asks preserve their choice value union, while any ask that permits free text returns string because the answer may be outside those choices."
      }
    ]
  },
  ".worktrees/control-theory-docs/packages/plugin/src/reconcile-context.ts::ReconcileContext": {
    "specs": [
      {
        "cases": [],
        "links": [
          "docs/plugin.md"
        ],
        "rules": [
          "Correlate verb continuation with the Core-issued execution identity, never with Resource identity or a caller-provided key; a crashed execution fails instead of replaying its call stack.",
          "Require one total timeout for every verb; draft and harness deadlines cover their Interaction gate and the resulting HarnessInvocation through terminal Turn."
        ],
        "spec": "Every Plugin verb first persists a Core-owned Interaction and suspends the same live Plugin execution until a terminal result; Baton never re-enqueues a Resource merely to deliver that result."
      }
    ]
  },
  ".worktrees/control-theory-docs/packages/plugin/src/reconcile-context.ts::VerbResult": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Treat deliberate negative answers as successful business values; reserve dismissed for Esc/close and failure for infrastructure or execution errors."
        ],
        "spec": "Every Plugin verb has exactly four terminal outcomes: success with a business value, explicit user dismissal, deadline timeout, or execution failure."
      }
    ]
  },
  ".worktrees/control-theory-docs/packages/plugin/src/resource.ts::ResourceClient.get": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "After awaiting a Core verb, use a uid-pinned ref before applying the result so a stale continuation cannot write to a replacement Resource."
        ],
        "spec": "A ResourceRef lookup returns the latest Resource only within the referenced namespace and, when uid is present, only for that exact incarnation; deletion or replacement returns undefined."
      }
    ]
  },
  ".worktrees/control-theory-docs/src/controller/index.ts::Controller.sendTurn": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../../docs/workflow.md"
        ],
        "spec": "每次用户提交只创建一个 Input/messageId；steer rejection 降级 follow-up 时保持身份不变。"
      }
    ]
  },
  ".worktrees/control-theory-docs/src/controller/input.ts::InputQueue.claimFirstForSteer": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../../docs/workflow.md"
        ],
        "spec": "只有仍位于队头的 Input 才能被 same-turn dispatch claim；claim 后不可被 composer recall。"
      }
    ]
  },
  ".worktrees/control-theory-docs/src/interaction/reconcile.ts::ReconcileInteractionStore": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep the Event Ledger as the durable fact store, not a continuation store: recovery must fail orphaned Plugin Interactions instead of reviving an old call stack."
        ],
        "spec": "A Plugin Interaction appends its first terminal fact to the Event Ledger before resolving any suspended continuation; later terminal attempts cannot replace it."
      }
    ]
  },
  ".worktrees/control-theory-docs/src/plugin/queue.ts::ReconcileCapacityLease": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "A Plugin verb wait must suspend both the Controller-local and Manager-global leases so one human wait cannot block unrelated Resources at either limit."
        ],
        "spec": "Suspending a reconcile releases its capacity slot for the full external wait and reacquires a slot before the original continuation resumes."
      }
    ]
  },
  ".worktrees/control-theory-docs/src/plugin/runner/client.ts::PluginRunnerClient": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Exclude a pending host-side Plugin verb from the generic Runner watchdog because the verb owns that wait's timeout; re-arm the watchdog after the host reply."
        ]
      }
    ]
  },
  ".worktrees/control-theory-docs/src/tui/protocol/state.ts::projectChatState": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Order status segments by volatility: stable model and configuration precede active phase or tool activity, while changing observations such as context usage stay at the end so frequent updates disturb only the right side."
        ]
      }
    ]
  },
  ".worktrees/terminal-native-links/packages/plugin/src/reconcile-context.ts::AskInput": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep allowOther as the type discriminant; allowOther:true must never expose a choice-only result type."
        ],
        "spec": "Closed-choice asks preserve their choice value union, while any ask that permits free text returns string because the answer may be outside those choices."
      }
    ]
  },
  ".worktrees/terminal-native-links/packages/plugin/src/reconcile-context.ts::ReconcileContext": {
    "specs": [
      {
        "cases": [],
        "links": [
          "docs/plugin.md"
        ],
        "rules": [
          "Correlate verb continuation with the Core-issued execution identity, never with Resource identity or a caller-provided key; a crashed execution fails instead of replaying its call stack.",
          "Require one total timeout for every verb; draft and harness deadlines cover their Interaction gate and the resulting HarnessInvocation through terminal Turn."
        ],
        "spec": "Every Plugin verb first persists a Core-owned Interaction and suspends the same live Plugin execution until a terminal result; Baton never re-enqueues a Resource merely to deliver that result."
      }
    ]
  },
  ".worktrees/terminal-native-links/packages/plugin/src/reconcile-context.ts::VerbResult": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Treat deliberate negative answers as successful business values; reserve dismissed for Esc/close and failure for infrastructure or execution errors."
        ],
        "spec": "Every Plugin verb has exactly four terminal outcomes: success with a business value, explicit user dismissal, deadline timeout, or execution failure."
      }
    ]
  },
  ".worktrees/terminal-native-links/packages/plugin/src/resource.ts::ResourceClient.get": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "After awaiting a Core verb, use a uid-pinned ref before applying the result so a stale continuation cannot write to a replacement Resource."
        ],
        "spec": "A ResourceRef lookup returns the latest Resource only within the referenced namespace and, when uid is present, only for that exact incarnation; deletion or replacement returns undefined."
      }
    ]
  },
  ".worktrees/terminal-native-links/src/interaction/reconcile.ts::ReconcileInteractionStore": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep the Event Ledger as the durable fact store, not a continuation store: recovery must fail orphaned Plugin Interactions instead of reviving an old call stack."
        ],
        "spec": "A Plugin Interaction appends its first terminal fact to the Event Ledger before resolving any suspended continuation; later terminal attempts cannot replace it."
      }
    ]
  },
  ".worktrees/terminal-native-links/src/plugin/queue.ts::ReconcileCapacityLease": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "A Plugin verb wait must suspend both the Controller-local and Manager-global leases so one human wait cannot block unrelated Resources at either limit."
        ],
        "spec": "Suspending a reconcile releases its capacity slot for the full external wait and reacquires a slot before the original continuation resumes."
      }
    ]
  },
  ".worktrees/terminal-native-links/src/plugin/runner/client.ts::PluginRunnerClient": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Exclude a pending host-side reconcile verb from the generic Runner watchdog because the verb owns that wait's timeout; re-arm the watchdog after the host reply."
        ]
      }
    ]
  },
  ".worktrees/terminal-native-links/src/tui/protocol/state.ts::projectChatState": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Order status segments by volatility: stable model and configuration precede active phase or tool activity, while changing observations such as context usage stay at the end so frequent updates disturb only the right side."
        ]
      }
    ]
  },
  ".worktrees/tool-call-effect/packages/plugin/src/reconcile-context.ts::AskInput": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep allowOther as the type discriminant; allowOther:true must never expose a choice-only result type."
        ],
        "spec": "Closed-choice asks preserve their choice value union, while any ask that permits free text returns string because the answer may be outside those choices."
      }
    ]
  },
  ".worktrees/tool-call-effect/packages/plugin/src/reconcile-context.ts::PluginVerbs": {
    "specs": [
      {
        "cases": [],
        "links": [
          "docs/plugin.md"
        ],
        "rules": [
          "Correlate verb continuation with the Core-issued execution identity, never with Resource identity or a caller-provided key; a crashed execution fails instead of replaying its call stack.",
          "Require one total timeout for every verb; draft and harness deadlines cover their Interaction gate and the resulting HarnessInvocation through terminal Turn."
        ],
        "spec": "Every Plugin verb first persists a Core-owned Interaction and suspends the same live Plugin execution until a terminal result; Baton never re-enqueues a Resource merely to deliver that result."
      }
    ]
  },
  ".worktrees/tool-call-effect/packages/plugin/src/reconcile-context.ts::VerbResult": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Treat deliberate negative answers as successful business values; reserve dismissed for Esc/close and failure for infrastructure or execution errors."
        ],
        "spec": "Every Plugin verb has exactly four terminal outcomes: success with a business value, explicit user dismissal, deadline timeout, or execution failure."
      }
    ]
  },
  ".worktrees/tool-call-effect/packages/plugin/src/resource.ts::ResourceClient.get": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "After awaiting a Core verb, use a uid-pinned ref before applying the result so a stale continuation cannot write to a replacement Resource."
        ],
        "spec": "A ResourceRef lookup returns the latest Resource only within the referenced namespace and, when uid is present, only for that exact incarnation; deletion or replacement returns undefined."
      }
    ]
  },
  ".worktrees/tool-call-effect/src/commands/registry.ts::CommandRegistry.resolve": {
    "specs": [
      {
        "cases": [],
        "spec": "Alias 只改变 slash 调用形式：它先绑定 canonical Command 参数；无剩余命令参数且显式允许 trailing text 的 Commandable，才把后缀作为独立 Prompt 返回。"
      }
    ]
  },
  ".worktrees/tool-call-effect/src/controller/index.ts::Controller.sendTurn": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../../docs/workflow.md"
        ],
        "spec": "每次用户提交只创建一个 Input/messageId；steer rejection 降级 follow-up 时保持身份不变。"
      }
    ]
  },
  ".worktrees/tool-call-effect/src/interaction/reconcile.ts::ReconcileInteractionStore": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep the Event Ledger as the durable fact store, not a continuation store: recovery must fail orphaned Plugin Interactions instead of reviving an old call stack."
        ],
        "spec": "A Plugin Interaction appends its first terminal fact to the Event Ledger before resolving any suspended continuation; later terminal attempts cannot replace it."
      }
    ]
  },
  ".worktrees/tool-call-effect/src/plugin/queue.ts::ReconcileCapacityLease": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "A Plugin verb wait must suspend both the Controller-local and Manager-global leases so one human wait cannot block unrelated Resources at either limit."
        ],
        "spec": "Suspending a reconcile releases its capacity slot for the full external wait and reacquires a slot before the original continuation resumes."
      }
    ]
  },
  ".worktrees/tool-call-effect/src/plugin/runner/client.ts::PluginRunnerClient": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Exclude a pending host-side Plugin verb from the generic Runner watchdog because the verb owns that wait's timeout; re-arm the watchdog after the host reply.",
          "Restore the host async causal scope by executionId before handling a Runner verb callback."
        ]
      }
    ]
  },
  ".worktrees/tool-call-effect/src/queue.ts::Queue.claimFirstForSteer": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../docs/workflow.md"
        ],
        "spec": "只有仍位于队头的 HarnessInput 才能被 same-turn dispatch claim；claim 后不可被 composer recall。"
      }
    ]
  },
  ".worktrees/tool-call-effect/src/view/chat-tui/protocol/index.ts::BatonChatProtocol.command": {
    "specs": [
      {
        "cases": [],
        "spec": "一次 Commandable 提交先完整执行并结算 canonical Command；只有成功后，允许的 trailing text 才作为下一条普通 Prompt 提交。"
      }
    ]
  },
  ".worktrees/tool-call-effect/src/view/chat-tui/protocol/state.ts::projectChatState": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Order status segments by volatility: stable model and configuration precede active phase or tool activity, while changing observations such as context usage stay at the end so frequent updates disturb only the right side."
        ]
      }
    ]
  },
  ".worktrees/tool-effect-layout/packages/plugin/src/reconcile-context.ts::AskInput": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep allowOther as the type discriminant; allowOther:true must never expose a choice-only result type."
        ],
        "spec": "Closed-choice asks preserve their choice value union, while any ask that permits free text returns string because the answer may be outside those choices."
      }
    ]
  },
  ".worktrees/tool-effect-layout/packages/plugin/src/reconcile-context.ts::PluginVerbs": {
    "specs": [
      {
        "cases": [],
        "links": [
          "docs/plugin.md"
        ],
        "rules": [
          "Correlate verb continuation with the Core-issued execution identity, never with Resource identity or a caller-provided key; a crashed execution fails instead of replaying its call stack.",
          "Require one total timeout for every verb; draft and harness deadlines cover their Interaction gate and the resulting HarnessInvocation through terminal Turn."
        ],
        "spec": "Every Plugin verb first persists a Core-owned Interaction and suspends the same live Plugin execution until a terminal result; Baton never re-enqueues a Resource merely to deliver that result."
      }
    ]
  },
  ".worktrees/tool-effect-layout/packages/plugin/src/reconcile-context.ts::VerbResult": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Treat deliberate negative answers as successful business values; reserve dismissed for Esc/close and failure for infrastructure or execution errors."
        ],
        "spec": "Every Plugin verb has exactly four terminal outcomes: success with a business value, explicit user dismissal, deadline timeout, or execution failure."
      }
    ]
  },
  ".worktrees/tool-effect-layout/packages/plugin/src/resource.ts::ResourceClient.get": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "After awaiting a Core verb, use a uid-pinned ref before applying the result so a stale continuation cannot write to a replacement Resource."
        ],
        "spec": "A ResourceRef lookup returns the latest Resource only within the referenced namespace and, when uid is present, only for that exact incarnation; deletion or replacement returns undefined."
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/commands/registry.ts::CommandRegistry.resolve": {
    "specs": [
      {
        "cases": [],
        "spec": "Alias 只改变 slash 调用形式：它先绑定 canonical Command 参数；无剩余命令参数且显式允许 trailing text 的 Commandable，才把后缀作为独立 Prompt 返回。"
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/controller/index.ts::Controller.sendTurn": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../../docs/workflow.md"
        ],
        "spec": "每次用户提交只创建一个 Input/messageId；steer rejection 降级 follow-up 时保持身份不变。"
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/interaction/reconcile.ts::ReconcileInteractionStore": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep the Event Ledger as the durable fact store, not a continuation store: recovery must fail orphaned Plugin Interactions instead of reviving an old call stack."
        ],
        "spec": "A Plugin Interaction appends its first terminal fact to the Event Ledger before resolving any suspended continuation; later terminal attempts cannot replace it."
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/plugin/queue.ts::ReconcileCapacityLease": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "A Plugin verb wait must suspend both the Controller-local and Manager-global leases so one human wait cannot block unrelated Resources at either limit."
        ],
        "spec": "Suspending a reconcile releases its capacity slot for the full external wait and reacquires a slot before the original continuation resumes."
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/plugin/runner/client.ts::PluginRunnerClient": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Exclude a pending host-side Plugin verb from the generic Runner watchdog because the verb owns that wait's timeout; re-arm the watchdog after the host reply.",
          "Restore the host async causal scope by executionId before handling a Runner verb callback."
        ]
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/queue.ts::Queue.claimFirstForSteer": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../docs/workflow.md"
        ],
        "spec": "只有仍位于队头的 HarnessInput 才能被 same-turn dispatch claim；claim 后不可被 composer recall。"
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/view/chat-tui/protocol/index.ts::BatonChatProtocol.command": {
    "specs": [
      {
        "cases": [],
        "spec": "一次 Commandable 提交先完整执行并结算 canonical Command；只有成功后，允许的 trailing text 才作为下一条普通 Prompt 提交。"
      }
    ]
  },
  ".worktrees/tool-effect-layout/src/view/chat-tui/protocol/state.ts::projectChatState": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Order status segments by volatility: stable model and configuration precede active phase or tool activity, while changing observations such as context usage stay at the end so frequent updates disturb only the right side."
        ]
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/packages/plugin/src/reconcile-context.ts::AskInput": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep allowOther as the type discriminant; allowOther:true must never expose a choice-only result type."
        ],
        "spec": "Closed-choice asks preserve their choice value union, while any ask that permits free text returns string because the answer may be outside those choices."
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/packages/plugin/src/reconcile-context.ts::PluginVerbs": {
    "specs": [
      {
        "cases": [],
        "links": [
          "docs/plugin.md"
        ],
        "rules": [
          "Correlate verb continuation with the Core-issued execution identity, never with Resource identity or a caller-provided key; a crashed execution fails instead of replaying its call stack.",
          "Require one total timeout for every verb; draft and harness deadlines cover their Interaction gate and the resulting HarnessInvocation through terminal Turn."
        ],
        "spec": "Every Plugin verb first persists a Core-owned Interaction and suspends the same live Plugin execution until a terminal result; Baton never re-enqueues a Resource merely to deliver that result."
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/packages/plugin/src/reconcile-context.ts::VerbResult": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Treat deliberate negative answers as successful business values; reserve dismissed for Esc/close and failure for infrastructure or execution errors."
        ],
        "spec": "Every Plugin verb has exactly four terminal outcomes: success with a business value, explicit user dismissal, deadline timeout, or execution failure."
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/packages/plugin/src/resource.ts::ResourceClient.get": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "After awaiting a Core verb, use a uid-pinned ref before applying the result so a stale continuation cannot write to a replacement Resource."
        ],
        "spec": "A ResourceRef lookup returns the latest Resource only within the referenced namespace and, when uid is present, only for that exact incarnation; deletion or replacement returns undefined."
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/commands/registry.ts::CommandRegistry.resolve": {
    "specs": [
      {
        "cases": [],
        "spec": "Alias 只改变 slash 调用形式：它先绑定 canonical Command 参数；无剩余命令参数且显式允许 trailing text 的 Commandable，才把后缀作为独立 Prompt 返回。"
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/controller/index.ts::Controller.sendTurn": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../../docs/workflow.md"
        ],
        "spec": "每次用户提交只创建一个 Input/messageId；steer rejection 降级 follow-up 时保持身份不变。"
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/interaction/reconcile.ts::ReconcileInteractionStore": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep the Event Ledger as the durable fact store, not a continuation store: recovery must fail orphaned Plugin Interactions instead of reviving an old call stack."
        ],
        "spec": "A Plugin Interaction appends its first terminal fact to the Event Ledger before resolving any suspended continuation; later terminal attempts cannot replace it."
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/plugin/queue.ts::ReconcileCapacityLease": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "A Plugin verb wait must suspend both the Controller-local and Manager-global leases so one human wait cannot block unrelated Resources at either limit."
        ],
        "spec": "Suspending a reconcile releases its capacity slot for the full external wait and reacquires a slot before the original continuation resumes."
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/plugin/runner/client.ts::PluginRunnerClient": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Exclude a pending host-side Plugin verb from the generic Runner watchdog because the verb owns that wait's timeout; re-arm the watchdog after the host reply.",
          "Restore the host async causal scope by executionId before handling a Runner verb callback."
        ]
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/queue.ts::Queue.claimFirstForSteer": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../docs/workflow.md"
        ],
        "spec": "只有仍位于队头的 HarnessInput 才能被 same-turn dispatch claim；claim 后不可被 composer recall。"
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/view/chat-tui/protocol/index.ts::BatonChatProtocol.command": {
    "specs": [
      {
        "cases": [],
        "spec": "一次 Commandable 提交先完整执行并结算 canonical Command；只有成功后，允许的 trailing text 才作为下一条普通 Prompt 提交。"
      }
    ]
  },
  ".worktrees/update-chat-tui-4da0b4d/src/view/chat-tui/protocol/state.ts::projectChatState": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Order status segments by volatility: stable model and configuration precede active phase or tool activity, while changing observations such as context usage stay at the end so frequent updates disturb only the right side."
        ]
      }
    ]
  },
  "packages/plugin/src/package.ts::PluginPackage": {
    "specs": [
      {
        "cases": [],
        "spec": "A Plugin Package does not declare scope or namespace; one enabled PluginInstance activates one Worker, while each Resource carries its own namespace."
      }
    ]
  },
  "packages/plugin/src/reconcile-context.ts::AskInput": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep allowOther as the type discriminant; allowOther:true must never expose a choice-only result type."
        ],
        "spec": "Closed-choice asks preserve their choice value union, while any ask that permits free text returns string because the answer may be outside those choices."
      }
    ]
  },
  "packages/plugin/src/reconcile-context.ts::PluginVerbs": {
    "specs": [
      {
        "cases": [],
        "links": [
          "docs/plugin.md"
        ],
        "rules": [
          "Correlate verb continuation with the Core-issued execution identity, never with Resource identity or a caller-provided key; a crashed execution fails instead of replaying its call stack.",
          "Require one total timeout for every verb; draft and harness deadlines cover their Interaction gate and the resulting HarnessInvocation through terminal Turn."
        ],
        "spec": "Every Plugin verb first persists a Core-owned Interaction and suspends the same live Plugin execution until a terminal result; Baton never re-enqueues a Resource merely to deliver that result."
      }
    ]
  },
  "packages/plugin/src/reconcile-context.ts::VerbResult": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Treat deliberate negative answers as successful business values; reserve dismissed for Esc/close and failure for infrastructure or execution errors."
        ],
        "spec": "Every Plugin verb has exactly four terminal outcomes: success with a business value, explicit user dismissal, deadline timeout, or execution failure."
      }
    ]
  },
  "packages/plugin/src/resource.ts::ResourceClient": {
    "specs": [
      {
        "cases": [],
        "spec": "One PluginInstance may own Resources in global, Project, and Session namespaces simultaneously; omitted mutation namespace defaults to `v1`."
      }
    ]
  },
  "packages/plugin/src/resource.ts::ResourceClient.get": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "After awaiting a Core verb, use a uid-pinned ref before applying the result so a stale continuation cannot write to a replacement Resource."
        ],
        "spec": "A ResourceRef lookup returns the latest Resource only within the referenced namespace and, when uid is present, only for that exact incarnation; deletion or replacement returns undefined."
      }
    ]
  },
  "src/commands/registry.ts::CommandRegistry.resolve": {
    "specs": [
      {
        "cases": [],
        "spec": "Alias 只改变 slash 调用形式：它先绑定 canonical Command 参数；无剩余命令参数且显式允许 trailing text 的 Commandable，才把后缀作为独立 Prompt 返回。"
      }
    ]
  },
  "src/controller/index.ts::Controller.sendTurn": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../../docs/workflow.md"
        ],
        "spec": "每次用户提交只创建一个 Input/messageId；steer rejection 降级 follow-up 时保持身份不变。"
      }
    ]
  },
  "src/interaction/reconcile.ts::ReconcileInteractionStore": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Keep the Event Ledger as the durable fact store, not a continuation store: recovery must fail orphaned Plugin Interactions instead of reviving an old call stack."
        ],
        "spec": "A Plugin Interaction appends its first terminal fact to the Event Ledger before resolving any suspended continuation; later terminal attempts cannot replace it."
      }
    ]
  },
  "src/plugin/queue.ts::ReconcileCapacityLease": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "A Plugin verb wait must suspend both the Controller-local and Manager-global leases so one human wait cannot block unrelated Resources at either limit."
        ],
        "spec": "Suspending a reconcile releases its capacity slot for the full external wait and reacquires a slot before the original continuation resumes."
      }
    ]
  },
  "src/plugin/runner/client.ts::PluginRunnerClient": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Exclude a pending host-side Plugin verb from the generic Runner watchdog because the verb owns that wait's timeout; re-arm the watchdog after the host reply.",
          "Restore the host async causal scope by executionId before handling a Runner verb callback."
        ]
      }
    ]
  },
  "src/queue.ts::Queue.claimFirstForSteer": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../docs/workflow.md"
        ],
        "spec": "只有仍位于队头的 HarnessInput 才能被 same-turn dispatch claim；claim 后不可被 composer recall。"
      }
    ]
  },
  "src/queue.ts::Queue.moveUserById": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../docs/workflow.md"
        ],
        "spec": "Queue reorder is recorded before the in-memory order changes, and user actions cannot cross HarnessInvocation work."
      }
    ]
  },
  "src/queue.ts::Queue.promoteUserById": {
    "specs": [
      {
        "cases": [],
        "links": [
          "../docs/workflow.md"
        ],
        "spec": "Dispatch-now preserves Input identity and may only promote across user-manageable queued work."
      }
    ]
  },
  "src/view/chat-tui/protocol/index.ts::BatonChatProtocol.command": {
    "specs": [
      {
        "cases": [],
        "spec": "一次 Commandable 提交先完整执行并结算 canonical Command；只有成功后，允许的 trailing text 才作为下一条普通 Prompt 提交。"
      }
    ]
  },
  "src/view/chat-tui/protocol/state.ts::projectChatState": {
    "specs": [
      {
        "cases": [],
        "rules": [
          "Order status segments by volatility: stable model and configuration precede active phase or tool activity, while changing observations such as context usage stay at the end so frequent updates disturb only the right side."
        ]
      }
    ]
  }
}
