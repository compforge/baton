{
  "src/plugin/interaction.ts::Store.ask": {
    "specs": [
      {
        "cases": [
          {
            "desc": "resolve an authored choice",
            "expect": "the AskResult value equals AskChoice.value",
            "id": "choice_value_round_trip"
          }
        ],
        "rules": [
          "Keep operation verb and key structured; do not encode the verb into the key."
        ],
        "spec": "Plugin ask identity keeps the authored key separate from the ask verb; choice values cross the Interaction boundary unchanged."
      }
    ]
  },
  "src/plugin/interaction.ts::Store.confirm": {
    "specs": [
      {
        "cases": [
          {
            "desc": "resolve a confirm choice",
            "expect": "accept becomes accepted and decline becomes declined",
            "id": "confirm_choice_mapping"
          }
        ],
        "rules": [
          "Keep operation verb and key structured; do not encode the verb into the key."
        ],
        "spec": "Plugin confirm identity keeps the authored key separate from the confirm verb and maps the fixed accept/decline choices to ConfirmResult states."
      }
    ]
  }
}
